
public class Test {
	public static void main (String[] args) {
		// 2.1
		double i = 50.0;
		double k = i + 50.0;
		double j = k + 1;
		
		System.out.println("j is " + j + " and"
				+ " k is " + k);
		
		// 2.7 initializing a constant int SIZE of value 20
		final int SIZE = 20;
		// 2.8 
		// MAX_VALUE follows convention for name of a constant
		// Test follows convention for name a of class
		// read follows convention for name of a variable
		// readDouble follows convention for name of a method
		
		//2.22 How would you write the following arithmetic expression in Java?
		// a) 4.0 / (3(r + 34) - 9(a + b * c) + ((3 + d * (2 + a)) / (a + b * d)
		// b) 5.5 * (Math.pow(2.5, (2.5 + t)))
		
	}
}
